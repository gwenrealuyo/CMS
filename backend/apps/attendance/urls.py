from django.urls import path
# from . import views

app_name = 'attendance'

urlpatterns = [
    # path('', views.AttendanceListCreate.as_view(), name='attendance-list-create'),
    # path('<int:pk>/', views.AttendanceRetrieveUpdateDestroy.as_view(), name='attendance-detail'),
]
