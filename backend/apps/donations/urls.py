from django.urls import path
# from . import views

app_name = 'donations'

urlpatterns = [
    # path('', views.DonationListCreate.as_view(), name='donation-list-create'),
    # path('<int:pk>/', views.DonationRetrieveUpdateDestroy.as_view(), name='donation-detail'),
]
