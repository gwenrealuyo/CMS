from django.urls import path
# from . import views

app_name = 'members'

urlpatterns = [
    # path('', views.MemberListCreate.as_view(), name='member-list-create'),
    # path('<int:pk>/', views.MemberRetrieveUpdateDestroy.as_view(), name='member-detail'),
]
