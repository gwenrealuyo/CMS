from django.urls import path
# from . import views

app_name = 'volunteers'

urlpatterns = [
    # path('', views.VolunteerListCreate.as_view(), name='volunteer-list-create'),
    # path('<int:pk>/', views.VolunteerRetrieveUpdateDestroy.as_view(), name='volunteer-detail'),
]
