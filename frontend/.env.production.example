# Copy to .env.production on the server (gitignored).
# Used when running: npm run build
#
# Must be a full URL (not /api alone). Rebuild after any change.

NEXT_PUBLIC_API_URL=https://api.example.com/api
