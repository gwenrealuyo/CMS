/** Hover hint shown on the locked branch control for branch-scoped users. */
export const REPORTS_BRANCH_LOCKED_HINT =
  "Branch is limited to your assignment. Admins and headquarters pastors can switch branches.";

/** Sentinel value for the "all branches" option (empty string = no filter). */
export const REPORTS_ALL_BRANCHES_VALUE = "";
